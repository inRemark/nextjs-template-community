# Next.js configuration
NEXT_PUBLIC_APP_NAME=SendMail
NEXT_PUBLIC_APP_DESCRIPTION=Professional email service platform for enterprises

# ===========================================
# 数据库配置 (必需)
# ===========================================
# PostgreSQL 数据库连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL="postgresql://username:password@localhost:5432/template_dev"

# ===========================================
# Redis 缓存配置 (可选，但推荐)
# ===========================================
# Redis 连接字符串 (推荐使用此方式)
# 格式: redis://用户名:密码@主机:端口/数据库号
# 示例: redis://user:password@localhost:6379/0
# 或者使用分离的配置项 (如果不想使用连接字符串)
REDIS_URL="redis://localhost:6379"
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# ===========================================
# 邮件服务配置 (可选)
# ===========================================
# SMTP 服务器配置
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
SMTP_FROM="noreply@your-domain.com"

# ===========================================
# NextAuth 认证配置 (必需)
# ===========================================
# 用于加密 JWT 和会话的密钥，至少32字符
NEXTAUTH_SECRET="your-super-secret-key-at-least-32-characters-long"
# 应用的基础URL，生产环境替换为实际域名
NEXTAUTH_URL="https://your-domain.com"

# ===========================================
# OAuth 第三方登录配置 (可选)
# ===========================================
# Google OAuth 2.0
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# 邮件服务提供商 (支持: gmail, outlook, sendgrid, resend)
EMAIL_PROVIDER="gmail"
# SendGrid API Key (如果使用 SendGrid)
SENDGRID_API_KEY="your-sendgrid-api-key"
# Resend API Key (如果使用 Resend)
RESEND_API_KEY="your-resend-api-key"

# Google Analytics
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
# Vercel Analytics
NEXT_PUBLIC_VERCEL_ANALYTICS="true"
